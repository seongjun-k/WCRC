# WCRC 주행 전략

## 한 줄 요약

**아루코 마커는 "어디서 멈출지"만 정하고, 그 사이 이동은 도로 마스크가 책임진다.
과수원·하차장은 들어가지 않고 코만 들이민다.**

---

## 코스 순서

```
START
 → 마커1 (교차로1)   → 90도 우회전 → 살짝 전진 → 사과 카운팅 → 후진 → 90도 좌회전
 → 마커2 (교차로2)   → 90도 좌회전 → 살짝 전진 → 사과 카운팅 → 후진 → 90도 우회전
 → 마커4 (교차로3)   → 90도 우회전 → 살짝 전진 → 사과 카운팅 → 후진 → 90도 좌회전
 → 마커5 (교차로4)   → 90도 좌회전 → 살짝 전진 → LCD 표시  → 후진 → 90도 우회전
                    → 2초 전진 → 3초 정지                     ← 횡단보도
 → 마커10           → 정지                                    ← END
```

찾아야 할 마커는 **5개**(1, 2, 4, 5, 10). 과수원 마커(3·6·9)와 하차장 마커(0)는
목표에서 뺐다.

---

## 왜 이렇게 했나

### 1. 과수원에 들어가지 않는다

규정 2번은 "진입했다는 **유의미한 행위**"만 요구한다. 90도 회전 + 전진이면 충족된다.

들어갔다 나오려면 180도 회전이 필요한데, 이 코스에서 제일 안 맞는 동작이다.
회전 시간이 오차나면 그게 그대로 진행 방향 오차가 되어 다음 마커를 못 찾는다.
**후진으로 빼면 헤딩을 아예 건드리지 않는다.**

부수 효과로 찾아야 할 마커가 9개 → 6개로 줄었다. 마커 하나당 실패 확률이 붙으므로
이게 완주 확률에 제일 크게 기여한다.

### 2. 도로 이탈은 흰 도로 마스킹으로 막는다

규정 패널티 4번: 두 바퀴가 도로 밖으로 나가면 **5초/회**.

맵 주석대로 메인도로 중앙선은 **삭제**됐다. 따라갈 선이 없으니 도로 면 자체를 딴다.
노란 테두리를 쓰려다 실제 사진 HSV를 재보니:

| | H | S | V |
|---|---|---|---|
| 흰 도로 | 80 | **24** | 223 |
| 초록 잔디 | 47 | 129 | 167 |
| 노란 테두리 | 47 | 132 | 149 |

**노란 선과 잔디는 색상(H)이 47로 똑같아서** 분리가 안 된다. 도로는 채도 24로 확연히
갈리므로 "저채도 + 고명도"로 딴다.

마커까지 전진할 때 눈 감고 직진하지 않고, 0.25초씩 쪼개서 매번 도로 중심을 보고
좌우 바퀴 속도를 다르게 준다. 먼 밴드·가까운 밴드 두 개를 봐서 곡선은 미리 꺾는다.

### 3. 횡단보도는 하차장 기준으로 시간을 재서 선다

횡단보도에는 아루코 마커가 없다. 그렇다고 STOP 표지판을 인식할 필요도 없다 —
**하차장 진입·복귀가 매번 같은 자리**라, 거기서 출발하는 지점도 그대로 재현되기 때문이다.
그러니 하차장에서 나온 뒤 `TO_CROSSWALK_TIME` 만큼 가서 3초 서면 된다.

참고로 표지판을 사과 모델에 같이 학습시키는 건 **안 된다**. Flask 서버는 클래스 구분 없이
**박스 개수만** 돌려주고(`app.py` 의 `detected_count = len(boxes)`) 모델도 한 개만 로드한다.
STOP 박스가 사과 개수에 더해져 규정 6번 숫자가 틀어진다 — 패널티 30초.

성공하면 보너스 -20초, 침범해도 -10초라 **일찍 서는 쪽이 항상 이득**이다.

### 4. 실패해도 멈추지 않는다

평가는 어디까지 갔느냐로 그룹이 갈린다 (메인도로 E < 교차로1 D < 과수원 C < 도착 B <
하차장 A). 마커 하나 놓쳤다고 전체를 포기하면 그룹이 통째로 내려간다.
**마커당 35초 제한**을 두고, 넘기면 포기하고 다음으로 넘어간다.
(규정 패널티 1번의 "60초 정체 시 기회 종료"에 안 걸리도록)

### 5. 사과는 빨간 것만

규정 3번 "잘 익은 사과(빨간 사과)를 카운팅". 서버는 클래스를 무시하고 **박스 개수만**
세므로, 모델을 1클래스로 만들고 **빨간 사과에만 박스를 친다**. 초록은 라벨하지 않는다.
개수가 틀리면 패널티 30초.

로봇이 3장 찍어 **최댓값**을 채택하므로 오탐 하나가 그대로 최종 점수가 된다.
→ 재현율보다 **정밀도** 우선.

---

## 대회 당일 순서

### 준비 (경기 전)

1. PC에서 `run.bat` 실행 → 브라우저에 `.pt` 드래그 앤 드롭
2. 로봇 Wi-Fi(`pinky_1186`)에 PC 연결 → PC IP 확인 → `drive.py` 의 `my_ip`
3. **코스 바닥에서 측정** (아래 "측정 4종")
4. `pinky-deploy` 로 drive.py 올리기
5. `pinky-run check` → 전부 OK 확인

### 측정 5종 (이게 전부다)

| 무엇 | 명령 |
|---|---|
| `MOVE_FORWARD_PER_ONE` | `pinky-run forward-cal` — 1초 전진 시 z가 몇 cm 주는지 |
| `TURN_QUATER_TIME` | `pinky-run turn-cal` — 90도 도는 데 몇 초인지 |
| `target_list` 의 x, z | `pinky-run pose` — 마커 6곳에서 로봇 세우고 값 읽기 |
| `ROAD_S_MAX` / `ROAD_V_MIN` | `pinky-run road` — 도로 위에서 |
| `TO_CROSSWALK_TIME` | 하차장에서 나와 횡단보도까지 몇 초인지 |

값을 고친 뒤 `pinky-deploy` 로 로봇에 다시 올린다.

`after_track_list` 의 **회전 방향**(좌/우)은 맵 기준으로 넣어뒀지만 코스에서 눈으로
확인할 것. 방향이 틀리면 반대편 허공을 보고 사과를 센다.

### 실행

심판 신호 → `pinky-run run` → **이후 키보드·마우스 접촉 금지**

---

## 검증 (코스 가기 전에 PC에서)

```bash
python tools/sim_drive.py    # 주행 로직 7가지 시나리오
python tools/road.py         # 도로 마스크 + 전진 분할
```

기회가 총 4번뿐이라 코스에서 처음 확인하는 건 낭비다. 둘 다 drive.py 를 그대로
읽어서 돌리므로 drive.py 를 고치면 같이 검증된다.

---

## 남은 위험

| 위험 | 대비 |
|---|---|
| 조명이 달라 도로 마스크가 깨짐 | `pinky-run road` 로 재조정. 못 찾으면 직진으로 폴백하게 돼 있다 |
| 회전 방향이 맵과 반대 | 코스에서 눈으로 확인 (제일 흔한 실수) |
| 마커를 못 찾음 | 35초 후 포기하고 다음으로. 양방향 부채꼴 탐색으로 ±50도까지 회수 확인됨 |
| 사과 개수 오차 | 학습 데이터가 아직 100장뿐. 대회장 조명에서 더 찍어 재학습 |
| 횡단보도 정지 위치가 어긋남 | `TO_CROSSWALK_TIME` 조정. 침범해도 -10초라 짧게 잡는 쪽이 안전 |
