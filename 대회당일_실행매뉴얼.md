# 대회 당일 최종 실행 매뉴얼

두 가지 방식을 다 준비해둔다. **1안이 기본, 2안은 백업**(1안이 안 될 때만).
둘 다 로봇 쪽 `drive.py`는 동일하게 쓴다 — 오늘 튜닝한 값(HOP, 회전 속도, END 라인,
사과 최댓값 집계 등)은 파일에 이미 들어있어서 코드를 안 건드리는 한 그대로 유지된다.

---

## 0. 공통 — 경기장 도착하면 제일 먼저

1. 로봇 전원 켜기, 노트북 전원 연결 (배터리 아끼지 말 것)
2. 노트북 wifi를 로봇 AP에 연결: `nmcli dev wifi connect pinky_1186 password wcrc2026pinky`
   (USB 랜카드가 있으면 `wrun`이 알아서 그쪽으로 붙는다)
3. 채널이 붐빌 수 있으니 한 번 정리: `wchan`
4. **PC IP 확인 — 경기장 wifi/DHCP 환경이라 어제 확인한 IP(`192.168.4.13`)와 다를 수 있다. 매번 새로 확인할 것:**
   ```bash
   ip -4 addr show $(nmcli -t -f DEVICE,CONNECTION dev | grep ':pinky_1186$' | cut -d: -f1) | grep -oP 'inet \K[0-9.]+'
   ```
   이 값을 아래 어느 방식에서든 그대로 쓴다. (로봇 자체 주소는 항상 `192.168.4.1`로 고정)

---

## 1안 — `wrun` (기본, 추천)

**장점**: 명령 하나로 끝(연결 확인·서버·업로드·점검까지 자동), 튜닝된 전처리(크롭/CLAHE/conf 0.15) 사용,
주행 후 사진+박스 이미지 자동 저장. **오늘 규정 확인 결과 이 방식도 문제 없음** (규정엔 실행 방식 제한 없음).

### 절차
1. 노트북 터미널에서: `wrun` (또는 바탕화면 "WCRC 주행" 아이콘)
2. 화면에 뜨는 점검 항목에 **FAIL이 없는지 확인**
3. "위에 FAIL 이 없으면 로봇을 START 선 안에 놓을 것" 문구 나오면 로봇 배치
4. **심판 출발 신호 → 그때 엔터** (이게 규정 1번의 "신호 후 코드 실행" 그 자체이므로 신호 전에 누르지 말 것 — 엔터는 이 한 번만 누르면 됨)
5. 주행이 끝나면 결과(사과 개수·시간 분해 등)가 화면에 뜨고, **바탕화면 아이콘으로 실행한 경우 10초 후 창이 자동으로 닫힌다** (더 안 눌러도 됨. 터미널에서 직접 `wrun`으로 실행했으면 이 대기 자체가 없다)

### 문제 생기면
- 중간에 멈춰야 함: `wstop`
- 서버 로그 확인: `tail -50 /tmp/wcrc-serve.log`
- 연결 문제: `nmcli device status`로 wifi 확인 후 다시 `wrun`

---

## 2안 — `app.py` 원본 + 로봇 주피터 터미널 (백업)

**언제 쓰나**: 1안이 이유 불문 안 될 때만. 대회 규정 원본 절차에 제일 가까운 방식이라 마음의 안전판.
**단점**: 수동 단계가 많고, 전처리 튜닝이 없어(conf 고정 0.25) 인식이 1안보다 둔감할 수 있음.
**⚠️ 어제 이 방식으로 "사과 0개"가 났던 이유 = IP를 안 넘겨서였다. 아래 6번을 절대 빠뜨리지 말 것.**

### 절차
1. 노트북에서 서버 실행:
   ```bash
   cd "/home/lsk0522/WCRC/wcrc 준비자료/260814_student/flask_server_wcrc"
   /home/lsk0522/WCRC/.venv/bin/python app.py
   ```
   브라우저가 자동으로 열린다 (안 열리면 `http://127.0.0.1:5000` 직접 접속).
2. 브라우저 Drop 영역에 학습된 `.pt` 드래그 앤 드롭:
   `/home/lsk0522/WCRC/pt_file/best.pt` (또는 `runs/detect/train/weights/best.pt`, 같은 파일)
3. `selected file`에 경로가 뜨면 성공.
4. 노트북에서 IP 재확인 (0번 참고) — 이 값을 6번에서 쓴다.
5. 브라우저로 로봇 접속: `http://192.168.4.1:8888` → **New → Terminal** (노트북 셀 아님)
6. 그 터미널에서 (IP는 4번 값으로 **반드시 교체**):
   ```bash
   WCRC_SERVER_IP=<4번에서 확인한 IP> python3 drive.py run
   ```
   `WCRC_SERVER_IP=`를 빠뜨리면 코드에 박힌 옛날 기본 IP로 접속을 시도해서 서버 연결이 실패하고
   **사과가 전부 0개로 나온다** (어제 실제로 겪은 증상 — 주행 자체는 정상인데 인식만 안 됨).
7. 엔터 대기 프롬프트가 뜨면, 여기서도 **심판 신호 → 엔터**.

---

## 두 방식 동시에 못 쓴다

포트 5000은 하나뿐이라 `tools/serve.py`(1안이 자동으로 씀)와 `app.py`(2안)를 동시에 띄울 수 없다.
2안을 테스트했다면 1안으로 돌아가기 전에 `app.py` 프로세스를 꺼야 `wrun`이 제대로 된 서버를 새로 띄운다:
```bash
kill $(ss -ltnp 2>/dev/null | grep 5000 | grep -oP 'pid=\K[0-9]+')
```

---

## 최종 체크리스트

- [ ] 로봇 배터리 완충
- [ ] 노트북 배터리/전원 확보
- [ ] `wchan` (채널 정리)
- [ ] wifi `pinky_1186` 연결 확인
- [ ] **PC IP 그 자리에서 재확인** (0번 명령)
- [ ] `wrun check` 한 번 돌려서 최신 코드·연결·점검 전부 OK 확인 (모터 안 돎, 안전)
- [ ] 1안이 안 되면 즉시 2안으로 전환 — 이 문서 들고 있을 것
- [ ] 2안 쓸 경우 `WCRC_SERVER_IP` 절대 안 빠뜨리기
