# WCRC 사과 인식 모델 학습 — 담당자 정리

준비자료(`wcrc 준비자료/`) + 핑키 공식 저장소(pinklab-art/pinky_study) 분석 결과.
**내 산출물은 `.pt` 파일 하나.** 로봇 코드·서버 코드는 건드리지 않는다.

---

## 1. 모델이 실제로 쓰이는 지점

```
로봇 카메라(640x480) ──JPEG──> PC Flask 서버 :5000/predict ──> YOLO(.pt) ──> 박스 개수만 응답
```

서버가 개수를 세는 코드 (`260814_student/flask_server_wcrc/app.py:330-336`):

```python
results = loaded_model(temp_path, conf=0.25)
boxes = results[0].boxes
detected_count = int(len(boxes)) if boxes is not None else 0
```

로봇이 개수를 채택하는 코드 (`pinkypro_wcrc_student.py:264-273`):

```python
for i in range(APPLE_CHECK_COUNT):        # 기본 3회 촬영
    count = send_image_and_get_count(frame)
    if count > temp_count: temp_count = count   # ★ 3장 중 최댓값 채택
```

**결론: 점수는 mAP가 아니라 "박스 개수가 정확히 맞느냐"로만 결정된다.**
클래스 라벨(red/green)은 서버에서 전혀 쓰이지 않는다.

---

## 2. 여기서 나오는 설계 결론 3가지

### (1) 1클래스(`apple`)로 학습할 것 — 2클래스는 개수 뻥튀기 위험

공식 가이드는 `names: [red, green], nc: 2`를 쓴다. 하지만 ultralytics의 NMS는
기본값(`agnostic_nms=False`)에서 **클래스별로 따로 동작**한다. 같은 사과 하나에
red 박스와 green 박스가 동시에 뜨면 NMS가 지우지 않고 **2개로 카운트**된다.
색 구분은 어차피 점수에 안 쓰이므로 `nc: 1, names: [apple]`이 안전하다.

**규정 확인 완료 (26wcrc_final.pdf 시나리오 3번): "과수원에서 잘 익은 사과(빨간 사과)를
카운팅".** 색을 따로 보고하는 게 아니라 **빨간 사과만 세는 것**이다. 서버는 클래스 무시하고
박스 개수만 세므로 결론은:

- `nc: 1, names: [apple]` 로 확정.
- 단 **라벨링 정책이 바뀐다 — 빨간(잘 익은) 사과에만 박스를 친다.**
  초록 사과는 박스 없이 그냥 둔다(암묵적 네거티브). 초록을 라벨하면 서버가 같이 세서
  개수가 틀리고 **패널티 30초**(규정 패널티 3번)를 먹는다.
- 초록 사과가 프레임에 많이 들어간 사진을 일부러 데이터에 넣어야 한다.
  "사과처럼 생겼지만 세면 안 되는 것"을 모델이 배우는 게 이 대회의 핵심 난이도다.

### (2) 오탐(False Positive)이 미탐보다 훨씬 치명적

3장 중 **최댓값**을 채택하므로, 3장 중 한 장에서만 헛박스가 하나 뜨면 그 값이 그대로 최종
개수가 된다. 반대로 한 장에서 하나 놓쳐도 다른 장이 잡으면 복구된다.
→ **정밀도(precision) 우선으로 튜닝.** 배경(잎, 빨간 테이프, 다른 로봇, 마커)을
사과로 오인하는 케이스를 데이터에 반드시 넣어 억제할 것.

### (3) conf는 서버에 0.25로 하드코딩

`app.py:330`. 바꾸려면 서버 담당이 파일 수정 후 재실행해야 한다(가이드 3장에도 명시).
**모델 쪽에서 conf 조정을 전제하지 말 것.** 0.25에서 헛박스가 안 나오게 학습으로 해결하고,
그래도 안 되면 서버 담당과 conf 상향을 협의.

---

## 3. 데이터 수집 사양 (로봇 코드에서 역산)

| 항목 | 값 | 근거 |
|---|---|---|
| 해상도 | 640×480 | picamera2 설정, pinky_study `06_picam.ipynb` |
| 포맷 | RGB888 → cv2 JPEG 인코딩 | `pinkypro_wcrc_student.py:196` |
| 상하 반전 | 카메라 프레임 180° 회전됨 | 공식 picam 예제 `cv2.rotate(...ROTATE_180)` |
| 촬영 위치 | 아루코마커 앞 정지 지점 (`target_list`의 x, z) | 실습가이드 6장 |
| 촬영 각도 | 로봇 눈높이 고정 (사람이 손으로 찍은 각도 아님) | 〃 |

**반드시 핑키 카메라로, 실제 코스의 그 정지 위치에서 촬영한다.**
휴대폰 사진이나 인터넷 사과 이미지로 학습하면 도메인이 달라 현장에서 무너진다.
`predict_apple_count()`가 도는 지점이 곧 학습 데이터 촬영 지점.

수집 시 변주해야 할 것: 대회장 조명, 사과 겹침(가림), 촬영 거리 z 값 ±, 좌우 정렬 x 값 ±,
배경에 다른 물체가 걸린 프레임(오탐 억제용 네거티브).

> 실습가이드 6장 경고: "촬영 각도가 조금만 틀어져도 사과가 가려져 개수가 달라진다."
> → 주행 담당이 `target_list`의 x/z를 확정하기 전이라도, 후보 지점 여러 개에서 미리 찍어두면
> 나중에 좌표가 바뀌어도 재수집이 덜 필요하다.

---

## 4. 데이터셋 구조 & 라벨링

공식 절차(pinky_study `1_pinky_blue/part3/03_yolo_learning.md`) 기준.

```
apple/
  data.yaml
  train/images/  train/labels/     # 8
  val/images/    val/labels/       # 2
```

- 도구: **labelImg v1.8.1** (경로에 한글 금지, C 드라이브에 압축 해제)
- `data/predefined_classes.txt`에 클래스명 입력 → 저장 (1클래스면 `apple` 한 줄)
- 포맷을 **PascalVOC → YOLO**로 반드시 변경 (labels가 `.txt`여야 함)
- 단축키: `w` 박스 그리기, `a`/`d` 이전/다음, `Ctrl+S` 저장 (저장 안 하면 라벨 유실)
- 라벨링 기준을 팀 내에서 하나로 고정할 것 — **가려진 사과를 어디까지 박스로 잡을지**가
  곧 최종 개수가 된다. (예: "절반 이상 보이면 라벨링" 같은 규칙을 정하고 전 이미지에 일관 적용)
- 최소 20장 이상이라고 되어 있으나, 개수 정확도가 목표이므로 **100장 이상 권장**

`apple/data.yaml` — 작성 완료. 다른 PC로 옮길 때 `path`만 바꾸면 된다.

```yaml
path: /home/ksj/orca/projects/WCRC/apple
train: train/images
val: val/images
nc: 1
names:
  - apple
```

(공식 가이드의 구글 드라이브 업로드 절차는 로컬 학습이므로 해당 없음.
 나중에 Colab을 쓸 일이 생기면 업로드 설정에서 "변환" 체크를 반드시 해제할 것 —
 안 하면 라벨 `.txt`가 `.gdoc`으로 바뀌어 통째로 날아간다.)

---

## 5. 학습 (로컬 RTX 4060 Laptop, 8GB)

```bash
pip install ultralytics          # torch(cu121) 같이 딸려온다
python tools/dataset.py train    # = yolov8s.pt, imgsz=640, batch=16, epochs=80
```

공식 예제(Colab)는 다음과 같은데, 로컬에서는 위 스크립트가 같은 일을 한다:

```python
from ultralytics import YOLO
model = YOLO("yolov8s.pt")
model.train(data="apple/data.yaml", epochs=30, imgsz=320)   # 공식 예제 값
```

**공식 예제에서 바꾼 값 2개:**

- `imgsz=320` → **`imgsz=640` 권장.** 서버는 `model(path, conf=0.25)`로 imgsz를 지정하지
  않고 호출하므로 ultralytics predict 기본값(640)으로 추론될 가능성이 크다. 학습 320 /
  추론 640이면 스케일이 어긋난다. 640으로 학습하면 어느 쪽이든 일치한다.
  (기본값이 학습 args를 승계하는지는 이 환경에서 검증 불가 — 640 학습이 손해가 없어 이쪽으로 간다.)
- `epochs=30` → 50~100. 데이터가 적으므로 과적합 확인하며 조정.

8GB VRAM에서 `batch=16`이 한계선이다. `CUDA out of memory`가 나면 `tools/dataset.py`의
`batch=16`을 8로 내린다. 결과물은 `runs/detect/train/weights/best.pt`.

---

## 6. 검증 — mAP 말고 개수로 검증한다

Colab 검증 예제는 이미지 1장 시각화만 한다:

```python
model = YOLO("/content/runs/detect/train/weights/best.pt")
results = model(cv2.imread(image_path))
cv2_imshow(results[0].plot())
```

**추가로 반드시 할 것: 서버와 동일 조건(conf=0.25)에서 개수를 맞춰보는 검증.**

```bash
python tools/dataset.py check     # val 전체의 "정답 개수 vs 예측 개수" 비교
```

불일치가 나면 **예측 > 정답(오탐)부터** 잡는다 (2절 (2) 이유).

최종 확인은 실기: 서버 실행 → `.pt` 드래그앤드롭 → 로봇으로 실제 지점에서
`APPLE_COUNT_ACTION` 돌려 LCD 숫자가 맞는지 본다.

---

## 7. 내 담당 체크리스트

- [x] 규정 확인 → 1클래스(`apple`) 확정, **빨간 사과만 라벨** (초록은 라벨 안 함)
- [ ] 주행 담당에게 사과 세는 지점(마커 id / 대략의 x, z) 받기
- [ ] 로봇에서 `python capture.py raw/images 100 1.0` 으로 촬영 (조명·가림·거리 변주 + 네거티브)
- [ ] `python tools/dataset.py label raw` 자동 라벨 → **labelImg 로 검수** (가림 기준 통일)
- [ ] `python tools/dataset.py split raw` 로 8:2 분할
- [ ] `python tools/dataset.py train` (로컬 4060, yolov8s / imgsz=640 / batch=16)
- [ ] `python tools/dataset.py check` — conf=0.25 기준 **개수 일치율**, 오탐 우선 제거
- [ ] `best.pt` 전달 + 서버 담당에게 "conf는 0.25 그대로" 또는 조정값 공유
- [ ] 대회장 조명에서 재촬영 → 필요시 추가 학습 (여분 시간 확보해둘 것)

---

## 참고 원본

- `wcrc 준비자료/WCRC_학생실습가이드.docx` — 2·3·6·9장이 모델 관련
- `wcrc 준비자료/260814_student/flask_server_wcrc/app.py` — 추론/카운트 로직
- `wcrc 준비자료/260814_student/pinky_pro_wcrc/pinkypro_wcrc_student.py` — 촬영/전송/채택
- https://github.com/pinklab-art/pinky_study `1_pinky_blue/part3/03_yolo_learning.md`, `04_yolo_test.ipynb`
