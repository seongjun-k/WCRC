# WCRC 명령어 매뉴얼

이 노트북(팀원 노트북) 기준. `~/.wcrc_aliases`가 새 터미널마다 자동 로드된다
(안 잡히면 `source ~/.wcrc_aliases`). `WCRC=/home/lsk0522/WCRC`.

README.md에 나오는 `wlabel`/`wsplit`/`wtrain`/`wcheck`/`pinky-*` alias는 **이
노트북에는 안 잡혀 있다** (팀원 노트북용 설치 스크립트가 그 alias들을 안 만든다).
아래 표에 그 대신 쓸 수 있는 실제 명령을 같이 적어뒀다.

---

## 1. 대회 당일 — 이 노트북 터미널에서

| 명령 | 하는 일 |
|---|---|
| `wrun` | 로봇 wifi 연결 → 서버 기동 → drive.py 업로드 → 점검 → **실주행**까지 한 번에 (출발은 엔터 1번) |
| `wruna` | `wrun`과 동일하지만 출발할 때 엔터도 안 기다린다 — 완전 무인 실행 (`drive.py run now`) |
| `wrun check` | 연결·서버·업로드·점검까지만 (모터 안 돎) |
| `wstop` | 주행 강제 중지 (로봇에서 drive.py 프로세스를 SSH로 kill) |
| `wchan` | 로봇 AP를 지금 자리에서 가장 빈 5GHz 채널로 변경 (대회장 도착 시 1회) |

`wrun`(실주행)이 끝나면 그 회차 사진을 자동으로 `apple_shots/<날짜시간횟수>/`에
받아온다 (실패해도 주행 결과에는 영향 없음, 경고만 출력).

---

## 2. 로봇에서 직접 (`ssh pinky@192.168.4.1` 후 `cd ~`)

`python3 drive.py <명령>` 형태. 모터가 도는 건 `run`뿐이고 나머지는 점검용.

| 명령 | 하는 일 |
|---|---|
| `check` | 출발 전 점검 (모터 안 돎) |
| `run` | 실주행 (준비 끝나고 엔터에서 출발) |
| `run now` | 엔터 없이 바로 출발 |
| `pose` | 마커 앞에 세워두고 실행 — 출력을 `target_list`에 그대로 붙여넣는 용도 |
| `motors` | 모터 방향(좌우) 확인. 반대면 코스 전체가 거꾸로 간다 — 제일 먼저 볼 것 |
| `forward-cal` | 마커가 정면에 보이는 자리에서 `MOVE_FORWARD_PER_ONE` 측정 |
| `turn-cal` | IMU로 닫은 회전이 실제로 맞는지 확인 |
| `road [사진경로]` | 도로 마스크 확인 (경로 생략하면 지금 카메라로) |
| `track <마커id> [target_z]` | 마커 하나만 찾아가서 서보기 |
| `actions <인덱스>` | `after_track_list`의 한 항목만 실행 (회전 방향·PEEK 시간 확인용) |
| `teleop [포트]` | 브라우저로 로봇을 몰면서 카메라·아루코 인식 실시간 확인 |

`wrun`을 안 쓰고 이 방식으로만 돌릴 때는 서버(`tools/serve.py`)가 미리 PC에서
떠 있어야 사과 인식이 된다.

---

## 3. 서버 (사과 인식, PC에서)

```bash
.venv/bin/python tools/serve.py                 # runs/detect/train/weights/best.pt 로 기동
.venv/bin/python tools/serve.py path/to/other.pt # 다른 .pt 지정
.venv/bin/python tools/serve.py --selftest       # 전처리 로직만 자체 점검 (서버 안 띄움)
```

`wrun`이 자동으로 띄워주므로 보통 직접 실행할 일은 없다. 껐다 다시 켜야 할 때만
(모델을 새로 학습했을 때 등):

```bash
# 켜져 있는 걸 찾아서 끄고
kill $(ss -ltnp 2>/dev/null | grep 5000 | grep -oP 'pid=\K[0-9]+')
# 다시 띄운다
(cd $WCRC && nohup .venv/bin/python tools/serve.py >/tmp/wcrc-serve.log 2>&1 &)
```

브라우저로 `http://<이 PC의 IP>:5000` 열면 최근 인식 결과 이미지를 볼 수 있다.

---

## 4. 사진 위치

| 위치 | 내용 |
|---|---|
| `apple_shots/<날짜시간횟수>/` | `wrun` 실주행마다 로봇에서 자동으로 받아온 원본 사진 (파일명 그대로) |
| `runs/server/predict/<날짜시간횟수>/` | 서버가 뜰 때마다 새로 생기는 폴더. 그 세션에 들어온 요청의 인식 결과(박스 그려진) 이미지 |
| `runs/auto_train/<날짜시간횟수>/` | `wautotrain` 실행마다 그 라운드 학습 결과(가중치·그래프·로그) 스냅샷 |
| `raw/images/`, `raw/labels/` | 학습용 데이터셋 원본 — 직접 채워야 한다 (아래 5번) |

로봇 쪽 사진을 수동으로 받고 싶으면:
```bash
ssh pinky@192.168.4.1 "ls -d ~/apple_shots/*/ | tail -1"   # 최근 회차 폴더 확인
scp -r pinky@192.168.4.1:~/apple_shots/<회차>/ $WCRC/apple_shots/
```

---

## 5. 학습 파이프라인 (PC, GPU 필요)

`apple_shots/`의 사진을 학습에 쓰려면 먼저 `raw/images/`로 모아야 한다(폴더 하나로
평평하게, 파일명 겹치지 않게):
```bash
for d in apple_shots/*/; do
  n=$(basename "$d")
  for f in "$d"*.jpg; do cp "$f" "raw/images/${n}_$(basename "$f")"; done
done
```

| 명령 | 하는 일 |
|---|---|
| `.venv/bin/python tools/redlabel.py raw` | 빨간 사과를 색으로 찾아 `raw/labels/`에 라벨 생성 (이미 있는 라벨은 기본적으로 안 덮음) |
| `.venv/bin/python tools/redlabel.py raw --force` | 기존 라벨 덮어쓰고 다시 라벨링 |
| `.venv/bin/python tools/redlabel.py raw --check` | `labelcheck/*.jpg`에 라벨 그려진 contact sheet 생성 + 의심 사진(라벨 0인데 빨간 덩어리 있음) 목록 출력 — **직접 눈으로 봐야 한다** |
| `.venv/bin/python tools/dataset.py split raw` | `raw` → `apple/train`, `apple/val` 8:2 분할 |
| `.venv/bin/python tools/dataset.py train [epochs]` | YOLO 학습 (기본 80 epoch). 결과를 `runs/detect/train/weights/best.pt`에 반영 |
| `.venv/bin/python tools/dataset.py check [모델]` | val 기준 "개수 일치율" 검증 (mAP 아님, 실전과 같은 채점 방식) |
| `.venv/bin/python tools/dataset.py label raw [모델] [conf]` | (참고용) YOLO 모델로 자동 라벨링 — 부트스트랩 모델 확신도가 낮아 사과엔 `redlabel.py`가 더 정확함 |
| `wautotrain [epochs]` | `raw/images`의 라벨 없는 사진을 (YOLO 부트스트랩으로) 자동 라벨 → 분할 → 학습을 한 번에. **라벨 검수 없이 도니 소량·단일 조건 데이터로 돌리면 과적합 위험 — 가능하면 `redlabel.py`로 먼저 라벨링해둔 뒤 돌릴 것** |

학습 끝나면 **반드시 서버를 재시작**해야 새 모델이 반영된다 (3번 참고).
재학습 전엔 지금 `best.pt`를 `pt_file/`에 복사해두는 걸 권장 (문제 생기면 되돌리기용).

---

## 6. 전처리 튜닝

```bash
wtune 1 2 1        # 과수원 A/B/C 의 실제 빨간 사과 개수를 인자로 (정답 기준 최적 전처리 탐색)
wtune              # 정답 없이, 지금 값으로 몇 개 세는지만 확인
wtune 1 2 1 --keep # 로봇에서 사진 새로 안 받아옴 (이미 tools/tune_shots/ 에 있는 걸로)
```
방금 `wrun` 실주행이 끝난 뒤 그 사진들로 동작한다. 결과는 `tools/tune.json`에 저장되고
다음 `wrun`이 서버를 자동으로 재기동해서 반영한다.

---

## 7. 실물 없이 검증 (PC)

| 명령 | 하는 일 |
|---|---|
| `.venv/bin/python tools/sim_drive.py` | `drive.py`를 가짜 하드웨어에 태워 7가지 시나리오 검증. **`drive.py`를 고치면 로봇에 올리기 전에 꼭 먼저 돌릴 것** |
| `.venv/bin/python tools/road.py` | 도로 마스크 로직을 `raw/images`의 사진들로 점검 |
| `.venv/bin/python tools/road.py <사진>` | 사진 한 장 진단 + 디버그 이미지 저장 |
| `.venv/bin/python tools/road.py --tune <사진>` | 그 사진에 맞는 `ROAD_S_MAX`/`ROAD_V_MIN` 추천 |

---

## 8. 로봇 원격 조작 (SSH 없이, Jupyter API)

```bash
.venv/bin/python tools/robot.py check                    # 연결 왕복 점검
.venv/bin/python tools/robot.py run "ls -al"              # 로봇에서 셸 명령
.venv/bin/python tools/robot.py py  "import cv2"          # 로봇에서 파이썬 코드
.venv/bin/python tools/robot.py put tools/capture.py capture.py
.venv/bin/python tools/robot.py get raw/images/a.jpg raw/images/a.jpg
.venv/bin/python tools/robot.py ls  [경로]
```
`PINKY_HOST` 환경변수로 로봇 주소 변경 가능 (기본 `192.168.4.1:8888`).

로봇에서 직접 촬영 (SSH 접속 후):
```bash
python3 capture.py [저장폴더] [장수] [간격초]   # 예: python3 capture.py raw/images 30 1.0
python3 capture_sweep.py [태그]                 # 과수원 A/B/C 앞에서, 제자리 회전 위주로 촬영
```

---

## 9. 지금 이 저장소에서 작동하지 않는 도구 (참고만 할 것)

`tools/map.py`, `tools/path.py`, `tools/cal.py`는 `drive.py`에 아직 없는 기능
(`run2`, `PATH_LEGS`, `cmd_cal` — "외운 경로로 주행" 관련 미구현 기능)을 전제로
한다. 실행하면 에러가 난다 (`map.py`는 `26wcrc_final.pdf`가 없어서, `cal.py`는
`drive.py`에 `cmd_cal`이 없어서). 지금 쓰는 마커 기반 주행(`drive.py`)과는
무관하니 무시해도 된다.

---

## 10. 문제 생겼을 때 우선 확인 순서

1. `ss -ltnp | grep 5000` — 서버 떠 있나
2. `curl -s -m 3 -o /dev/null -w '%{http_code}\n' http://127.0.0.1:5000/` — 서버 응답하나
3. `tail -50 /tmp/wcrc-serve.log` — 서버 쪽 에러
4. `nmcli device status` — wifi 가 `pinky_1186`에 붙어있나
5. `.venv/bin/python tools/sim_drive.py` — drive.py 자체 로직 문제인지 먼저 걸러내기
